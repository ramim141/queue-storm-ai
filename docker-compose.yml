version: "3.9"

services:

  api:

    build: .

    container_name: queue_storm_ai

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app

    command: >
      python manage.py runserver 0.0.0.0:8000